# 실제 값은 여기 적지 않는다.
# 로컬 개발: 이 파일을 .env.local 로 복사한 뒤 값을 채우고 `npx vercel dev` 실행
# 배포: 버셀 대시보드 → Settings → Environment Variables 에 등록

# 카카오 REST API 키 (주소 검색, 실제 도로 경로)
KAKAO_REST_KEY=

# 이용자 관리 (README의 "이용자 관리" 항목 참고)
AUTH_SECRET=
ADMIN_PASSWORD=
EDGE_CONFIG=
EDGE_CONFIG_ID=
VERCEL_API_TOKEN=
